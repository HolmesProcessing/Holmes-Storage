The following are a list of queries that we regularly encounter. Based on this information we should validate the data model we are using.

# API
1) Result
    - via UUID
2) Results
    - via sha
    - via service name 
    - via time range
3) Submission
    - via UUID
4) Submissions
    - vis sha [should we make everything sha256..i.e. domain, ip, etc all get a sha256?]
    - via time_time range
    - via source
    - via tag?
    - via user_id
5) Object
    - vis sha [should we make everything sha256..i.e. domain, ip, etc all get a sha256?]
6) Objects
    - via type [should we breakthis down to include file_mime, tld, domain_part, ip_v6?]
    - via time range
    - via source
    - via user
7) Config

## Proposed API scheme
### GET
Get one item back from the server
* v2/object/sha256
* v2/raw_data/sha256
* v2/result
* v2/submission
* v2/config

* v2/objects?{uuid, [identifier], mime}
* v2/results?{uuid, sha256, service_name}
* v2/submissions?{source, date}

### PATCH
* v2/object/sha256

### POST
* v2/object
* v2/raw_data
* v2/result
* v2/submissions
* v2/config

### DELETE
* v2/object:sha256
* v2/raw_data
* v2/result
* v2/submission

# ML
1) Get all results from a service_name
2) Get all objects from a mime type
